# วิธีติดตั้ง DTP NST GAS Library แบบง่าย (ใช้ Active Spreadsheet)

## 🎯 ภาพรวม

Library นี้จะใช้ข้อมูลจาก **Spreadsheet ที่ติดตั้ง Library** โดยตรง ไม่ต้องตั้งค่า SPREADSHEET_ID แยก ทำให้ติดตั้งง่ายและรวดเร็วขึ้น!

---

## 📋 ขั้นตอนการติดตั้ง (5 ขั้นตอนง่ายๆ)

### ขั้นที่ 1: สร้าง Spreadsheet
1. เปิด Google Drive
2. สร้าง Google Spreadsheet ใหม่
3. ตั้งชื่อ เช่น **"DTP NST Database"**

---

### ขั้นที่ 2: เปิด Apps Script Editor
1. จาก Spreadsheet: **Extensions > Apps Script**
2. ลบโค้ดเริ่มต้นทั้งหมด

---

### ขั้นที่ 3: คัดลอกไฟล์โค้ด
1. สร้างไฟล์ใหม่สำหรับแต่ละ module:
   - คลิก **+ > Script file** แล้วตั้งชื่อ `Auth`
   - ทำซ้ำสำหรับ: `Authorization`, `CRUD`, `Utils`, `Admin`
2. ไฟล์ `Code.gs` ที่มีอยู่แล้ว - แก้ไขเนื้อหา
3. คัดลอกโค้ดจาก repository ไปยังแต่ละไฟล์
4. ตั้งค่า `appsscript.json`:
   - **Project Settings** (⚙️) > เลือก **Show "appsscript.json" manifest file in editor**
   - คัดลอกเนื้อหาจาก `appsscript.json`

---

### ขั้นที่ 4: รัน Setup (ง่ายมาก!)

#### 4.1 สร้างตารางข้อมูล
คัดลอกโค้ดนี้ลงใน Apps Script:

```javascript
function runSetup() {
  const result = setupLibrary();
  Logger.log(result);
}
```

**รันฟังก์ชัน:**
1. เลือก `runSetup` จาก dropdown
2. คลิก **Run**
3. อนุญาตสิทธิ์เมื่อถูกถาม
4. ดูผลใน **Execution log** (ควรเห็น success)

✅ **ตรวจสอบ:** กลับไปที่ Spreadsheet จะเห็น 8 sheets ใหม่

---

#### 4.2 สร้าง Admin User
```javascript
function runCreateAdmin() {
  const result = createInitialAdmin('admin', 'YourPassword123', 'ผู้ดูแลระบบ');
  Logger.log(result);
}
```

**รันฟังก์ชัน `runCreateAdmin`**

📝 **บันทึกข้อมูล:**
- Username: `admin`
- Password: `YourPassword123` (เปลี่ยนเป็นรหัสที่ปลอดภัย)

---

#### 4.3 ลงทะเบียน Application
```javascript
function runRegisterApp() {
  const result = registerApplication(
    'ระบบจัดการบุคลากร',
    'แอปพลิเคชันหลักสำหรับจัดการข้อมูล',
    ''
  );
  Logger.log(result);
}
```

**รันฟังก์ชัน `runRegisterApp`**

🔑 **สำคัญมาก:** คัดลอก `app_key` จาก log เก็บไว้ใช้งาน!

---

#### 4.4 ทดสอบระบบ
```javascript
function runTest() {
  const result = testLibrarySetup();
  Logger.log(result);
}
```

**รันฟังก์ชัน `runTest`**

ตรวจสอบว่าทุก test เป็น **PASS** ✅

---

### ขั้นที่ 5: Deploy เป็น Library (ถ้าต้องการแชร์)

**ถ้าต้องการใช้เฉพาะใน Spreadsheet นี้:**
- ไม่ต้อง deploy! ใช้ฟังก์ชันได้เลย

**ถ้าต้องการให้แอปอื่นใช้งาน:**
1. **Deploy > New deployment**
2. Type: **Library**
3. Description: `DTP NST GAS Library v1.0`
4. Access: เลือกตามต้องการ
5. คลิก **Deploy**
6. คัดลอก **Script ID** เก็บไว้

---

## 🎯 วิธีใช้งาน

### แบบที่ 1: ใช้งานภายใน Spreadsheet เดียวกัน (ง่ายที่สุด)

ไม่ต้อง deploy! สร้างฟังก์ชันใน Apps Script ได้เลย:

```javascript
function testMyLibrary() {
  // 1. ขอ token
  const tokenResult = request_token({
    username: 'admin',
    password: 'YourPassword123'
  }, 'admin');
  
  Logger.log('Token:', tokenResult);
  
  if (!tokenResult.success) {
    Logger.log('ล้มเหลว:', tokenResult.message);
    return;
  }
  
  // 2. เชื่อมต่อ (ใช้ app_key ที่ได้จากขั้นตอน 4.3)
  const conn = connect('app_key_ของคุณ', tokenResult.token);
  
  if (!conn.success) {
    Logger.log('เชื่อมต่อล้มเหลว:', conn.message);
    return;
  }
  
  // 3. ทดสอบอ่านข้อมูล
  const positions = conn.read('positions', {});
  Logger.log('Positions:', positions);
  
  // 4. ทดสอบสร้างข้อมูล
  const newPos = conn.create('positions', {
    name: 'นักวิชาการคอมพิวเตอร์',
    level: 7
  });
  Logger.log('Created:', newPos);
  
  Logger.log('✅ ใช้งานสำเร็จ!');
}
```

---

### แบบที่ 2: ใช้งานจาก Apps Script อื่น (ต้อง deploy ก่อน)

1. เปิด Apps Script project อื่น
2. **Libraries (+)** > Add a library
3. ใส่ **Script ID** จากขั้นที่ 5
4. Identifier: `DTPNSTLib`
5. เลือก version
6. คลิก **Add**

จากนั้นใช้งานแบบนี้:

```javascript
function useLibraryFromAnotherProject() {
  // ใช้ DTPNSTLib. นำหน้า
  const tokenResult = DTPNSTLib.request_token({
    username: 'admin',
    password: 'YourPassword123'
  }, 'admin');
  
  const conn = DTPNSTLib.connect('your_app_key', tokenResult.token);
  
  const data = conn.read('users', {});
  Logger.log(data);
}
```

---

## 📝 ตัวอย่างการใช้งานจริง

### สร้างข้อมูลองค์กรและผู้ใช้

```javascript
function createSampleData() {
  // ขอ token
  const tokenResult = request_token({
    username: 'admin',
    password: 'YourPassword123'
  }, 'admin');
  
  const conn = connect('your_app_key', tokenResult.token);
  
  // 1. สร้างองค์กร
  const org = conn.create('organizations', {
    hrms_id: 'HRMS001',
    org_name: 'กรมสมเด็จพระเจ้าตากสินมหาราช',
    district: 'เขตคลองเตย',
    province: 'กรุงเทพมหานคร'
  });
  
  Logger.log('สร้างองค์กร:', org);
  
  // 2. สร้างตำแหน่ง
  const position = conn.create('positions', {
    name: 'นักวิชาการคอมพิวเตอร์',
    level: 7
  });
  
  Logger.log('สร้างตำแหน่ง:', position);
  
  // 3. สร้างยศ
  const rank = conn.create('ranks', {
    name: 'ชำนาญการพิเศษ',
    abbreviation: 'ชกพ.',
    level: 8
  });
  
  Logger.log('สร้างยศ:', rank);
  
  // 4. สร้างผู้ใช้
  const user = conn.create('users', {
    name: 'สมชาย ใจดี',
    id13: '1234567890123',
    password: 'User123456',
    position_id: position.data.uuid,
    rank_id: rank.data.uuid,
    org_id: org.data.uuid
  });
  
  Logger.log('สร้างผู้ใช้:', user);
  Logger.log('✅ สร้างข้อมูลเสร็จสิ้น!');
}
```

### อ่านข้อมูลทั้งหมด

```javascript
function readAllData() {
  const tokenResult = request_token({
    username: 'admin',
    password: 'YourPassword123'
  }, 'admin');
  
  const conn = connect('your_app_key', tokenResult.token);
  
  // อ่านข้อมูลแต่ละตาราง
  const organizations = conn.read('organizations', {});
  const positions = conn.read('positions', {});
  const ranks = conn.read('ranks', {});
  const users = conn.read('users', { active: true });
  
  Logger.log('องค์กร:', organizations.data.length, 'รายการ');
  Logger.log('ตำแหน่ง:', positions.data.length, 'รายการ');
  Logger.log('ยศ:', ranks.data.length, 'รายการ');
  Logger.log('ผู้ใช้:', users.data.length, 'คน');
}
```

---

## ✅ สิ่งที่เปลี่ยนแปลง

### เดิม (ซับซ้อน)
```javascript
// ต้องตั้งค่า SPREADSHEET_ID
function runSetup() {
  const spreadsheetId = 'ใส่_ID_ยาวๆ_ที่นี่';
  const result = setupLibrary(spreadsheetId);
  Logger.log(result);
}
```

### ใหม่ (ง่ายขึ้น) ✨
```javascript
// ไม่ต้องใส่ SPREADSHEET_ID
function runSetup() {
  const result = setupLibrary(); // แค่นี้!
  Logger.log(result);
}
```

---

## 🎯 ข้อดีของการใช้ Active Spreadsheet

✅ **ติดตั้งง่ายขึ้น** - ไม่ต้องคัดลอก Spreadsheet ID  
✅ **ไม่มีข้อผิดพลาดจาก ID ผิด** - ใช้ spreadsheet ปัจจุบันอัตโนมัติ  
✅ **เหมาะสำหรับ Bound Script** - Library ติดกับ spreadsheet  
✅ **ทดสอบง่าย** - เขียนฟังก์ชันทดสอบใน spreadsheet เดียวกัน  
✅ **แยก Environment ได้** - แต่ละ spreadsheet = แต่ละ environment  

---

## 🐛 แก้ปัญหา

### ❌ "No active spreadsheet found"
**สาเหตุ:** พยายามรันนอก spreadsheet context

**วิธีแก้:**
- รันฟังก์ชันจาก Apps Script Editor ที่เปิดจาก spreadsheet
- หรือ deploy เป็น library แล้วใช้จาก bound script อื่น

---

### ❌ Sheets ไม่ถูกสร้าง
**วิธีแก้:**
1. กลับไปที่ spreadsheet
2. รีเฟรชหน้าเว็บ
3. รัน `setupLibrary()` อีกครั้ง

---

### ❌ ฟังก์ชันหาไม่เจอ (ใช้จาก project อื่น)
**วิธีแก้:**
- ต้อง deploy เป็น library ก่อน
- เรียกใช้ด้วย `DTPNSTLib.` นำหน้า

---

## 📚 เอกสารเพิ่มเติม

- **README_TH.md** - คู่มือฉบับเต็ม (ภาษาไทย)
- **Example.gs** - ตัวอย่างการใช้งาน 6 แบบ
- **SCHEMA.md** - โครงสร้างฐานข้อมูล

---

## 🎉 เริ่มใช้งานได้เลย!

1. ✅ รัน `setupLibrary()`
2. ✅ รัน `createInitialAdmin()`
3. ✅ รัน `registerApplication()`
4. ✅ รัน `testLibrarySetup()`
5. ✅ ทดสอบด้วย `testMyLibrary()`

**เสร็จแล้ว! 🚀**

---

**Version:** 1.0.0 (Active Spreadsheet)  
**Updated:** November 2025  
**DTP NST Team**
