# 📚 DTP NST GAS Library - คู่มือภาษาไทย

> Google Apps Script Library สำหรับจัดการข้อมูล Account พร้อมระบบ Authentication และ Authorization

## 🎯 ความสามารถหลัก

- ✅ ระบบ Authentication แบบ Token
- ✅ ระบบ Authorization ตาม Role (Admin/User)
- ✅ CRUD Operations สำหรับทุกตาราง
- ✅ Audit Logging บันทึกทุกการทำงาน
- ✅ ความปลอดภัยระดับสูง (Password hashing, Token expiration)

## 📦 ไฟล์ในโปรเจกต์

### ไฟล์ Code (สำหรับ Apps Script)
- `Code.gs` - ไฟล์หลัก (Public API)
- `Auth.gs` - ระบบ Authentication
- `Authorization.gs` - ระบบ Authorization
- `CRUD.gs` - ระบบ Create, Read, Update, Delete
- `Utils.gs` - ฟังก์ชันช่วยเหลือต่างๆ
- `Admin.gs` - ฟังก์ชันสำหรับ Admin
- `appsscript.json` - การตั้งค่า Project

### ไฟล์เอกสาร
- `README.md` - คู่มือภาษาอังกฤษ (ฉบับเต็म)
- `README_TH.md` - คู่มือภาษาไทย (ไฟล์นี้)
- `SETUP_TH.md` - คู่มือติดตั้งแบบละเอียด (ภาษาไทย)
- `SETUP_CHECKLIST.md` - Checklist การติดตั้ง
- `SCHEMA.md` - โครงสร้างฐานข้อมูล
- `ARCHITECTURE.md` - สถาปัตยกรรมระบบ
- `PROJECT_SUMMARY.md` - สรุปโปรเจกต์

### ไฟล์ตัวอย่าง
- `Example.gs` - ตัวอย่างการใช้งานแบบเต็มรูปแบบ

## 🚀 เริ่มต้นใช้งาน

### ขั้นตอนสั้นๆ

1. **สร้าง Spreadsheet** และคัดลอก ID
2. **สร้าง Apps Script Project** และคัดลอกไฟล์ทั้งหมด
3. **รัน Setup Functions** ตามลำดับ
4. **Deploy เป็น Library**
5. **เริ่มใช้งาน!**

### คู่มือโดยละเอียด

อ่านได้ที่: [SETUP_TH.md](./SETUP_TH.md) - มีคำแนะนำทีละขั้นตอนแบบละเอียด

หรือใช้: [SETUP_CHECKLIST.md](./SETUP_CHECKLIST.md) - มี Checklist สำหรับติ๊กทำไปทีละขั้นตอน

## 💡 ตัวอย่างการใช้งานง่ายๆ

### 1. ขอ Token

```javascript
// สำหรับ Admin
const result = DTPNSTLib.request_token({
  username: 'admin',
  password: 'yourpassword'
}, 'admin');

console.log(result.token); // เก็บ token ไว้ใช้

// สำหรับ User
const result = DTPNSTLib.request_token({
  id13: '1234567890123',
  password: 'userpassword'
}, 'user');
```

### 2. เชื่อมต่อ Library

```javascript
const appKey = 'app_your_app_key_here';
const token = 'your_token_here';

const conn = DTPNSTLib.connect(appKey, token);
```

### 3. ใช้งาน CRUD

```javascript
// สร้างข้อมูล
const org = conn.create('organizations', {
  hrms_id: 'HRMS001',
  org_name: 'กรมสมเด็จพระเจ้าตากสินมหาราช',
  province: 'กรุงเทพมหานคร'
});

// อ่านข้อมูล
const users = conn.read('users', { active: true });

// แก้ไขข้อมูล
const updated = conn.update('users', userId, {
  name: 'ชื่อใหม่'
});

// ลบข้อมูล (Soft delete)
const deleted = conn.delete('users', userId);
```

## 🔐 สิทธิ์การเข้าถึง

### Admin
- ✅ สร้าง, อ่าน, แก้ไข, ลบ ได้ทุกตาราง
- ✅ จัดการ users, organizations, positions, ranks
- ✅ จัดการ admins, applications
- ✅ ดู logs ทั้งหมด

### User (ผู้ใช้ทั่วไป)
- ✅ อ่านข้อมูล users ในองค์กรของตัวเอง
- ✅ อ่านข้อมูล organization ของตัวเอง
- ✅ อ่านข้อมูล positions และ ranks ทั้งหมด
- ❌ ไม่สามารถสร้าง, แก้ไข, ลบ ได้
- ❌ ไม่สามารถดู logs, admins, applications

## 📊 โครงสร้างฐานข้อมูล

### ตารางหลัก

1. **users** - ข้อมูลผู้ใช้งาน
   - uuid, name, id13, password
   - position_id, rank_id, org_id
   - active, created_at, updated_at

2. **organizations** - ข้อมูลองค์กร
   - uuid, hrms_id, dmz_id, org_name
   - subdistrict, district, province
   - active, created_at, updated_at

3. **positions** - ตำแหน่งงาน
   - uuid, name, description, level

4. **ranks** - ยศ/ระดับ
   - uuid, name, abbreviation, level

5. **logs** - บันทึกการทำงาน
   - user_id13, action, table_name
   - record_id, status, app_id

6. **admins** - ผู้ดูแลระบบ
   - uuid, username, password
   - name, role, active

7. **applications** - แอปพลิเคชันที่ลงทะเบียน
   - uuid, appname, app_key
   - description, active

8. **tokens** - Token สำหรับ Authentication
   - uuid, token, user_type
   - user_id, expires_at, revoked

รายละเอียดเต็มดูที่: [SCHEMA.md](./SCHEMA.md)

## 🛠️ ฟังก์ชันสำหรับ Admin

### Setup Functions (รันครั้งแรก)

```javascript
// 1. ตั้งค่า Library
setupLibrary('your-spreadsheet-id');

// 2. สร้าง Admin คนแรก
createInitialAdmin('admin', 'password123', 'ผู้ดูแลระบบ');

// 3. ลงทะเบียนแอพ
registerApplication('My App', 'คำอธิบาย', '');

// 4. ทดสอบการติดตั้ง
testLibrarySetup();
```

### Maintenance Functions (รันเป็นประจำ)

```javascript
// ทำความสะอาดอัตโนมัติ (ตั้ง Trigger รายวัน)
dailyMaintenance();

// ดูสถิติการใช้งาน
getLibraryStats();
```

## 📱 วิธีใช้ใน App ของคุณ

### เพิ่ม Library

1. เปิด Apps Script Project ของคุณ
2. **Libraries (+)** > Add a library
3. **Script ID:** วาง Script ID ของ Library
4. **Identifier:** `DTPNSTLib`
5. **Version:** เลือก version ล่าสุด
6. **Add**

### เขียนโค้ดใช้งาน

```javascript
function myFunction() {
  // 1. ขอ token
  const tokenResult = DTPNSTLib.request_token({
    username: 'admin',
    password: 'yourpassword'
  }, 'admin');
  
  if (!tokenResult.success) {
    console.log('ไม่สามารถขอ token ได้');
    return;
  }
  
  // 2. เชื่อมต่อ
  const conn = DTPNSTLib.connect('your-app-key', tokenResult.token);
  
  if (!conn.success) {
    console.log('ไม่สามารถเชื่อมต่อได้');
    return;
  }
  
  // 3. ใช้งาน CRUD
  const result = conn.read('users', {});
  console.log('พบผู้ใช้:', result.data.length, 'คน');
}
```

## 🔒 ความปลอดภัย

### การเก็บข้อมูลที่ปลอดภัย

ใช้ Script Properties แทนการเขียน hardcode:

```javascript
// ตั้งค่าครั้งแรก
function setup() {
  const props = PropertiesService.getScriptProperties();
  props.setProperty('APP_KEY', 'your-app-key');
  props.setProperty('ADMIN_USER', 'admin');
  props.setProperty('ADMIN_PASS', 'password');
}

// ใช้งาน
function useSecurely() {
  const props = PropertiesService.getScriptProperties();
  
  const result = DTPNSTLib.request_token({
    username: props.getProperty('ADMIN_USER'),
    password: props.getProperty('ADMIN_PASS')
  }, 'admin');
  
  // ...
}
```

### Best Practices

1. ✅ ใช้รหัสผ่านที่แข็งแรง (อย่างน้อย 8 ตัวอักษร)
2. ✅ เก็บ app_key และ password ใน Script Properties
3. ✅ เปลี่ยนรหัสผ่านเป็นประจำ
4. ✅ Revoke token เมื่อใช้งานเสร็จ
5. ✅ ตรวจสอบ logs เป็นระยะ

## ❓ แก้ปัญหาที่พบบ่อย

### "Spreadsheet not configured"
```javascript
setupLibrary('your-spreadsheet-id');
```

### "Invalid token"
```javascript
// Token หมดอายุ - ขอใหม่
const result = DTPNSTLib.request_token(credentials, userType);
```

### "Access denied"
- ตรวจสอบ user type (admin/user)
- ตรวจสอบสิทธิ์การเข้าถึงตาราง

### "Invalid app key"
- ตรวจสอบ app_key จากการลงทะเบียน
- หรือลงทะเบียนแอพใหม่

## 📖 เอกสารเพิ่มเติม

- **[SETUP_TH.md](./SETUP_TH.md)** - คู่มือติดตั้งแบบละเอียด
- **[SETUP_CHECKLIST.md](./SETUP_CHECKLIST.md)** - Checklist ติดตั้ง
- **[Example.gs](./Example.gs)** - ตัวอย่างโค้ด 6 แบบ
- **[SCHEMA.md](./SCHEMA.md)** - โครงสร้างฐานข้อมูล
- **[ARCHITECTURE.md](./ARCHITECTURE.md)** - สถาปัตยกรรมระบบ
- **[README.md](./README.md)** - เอกสารภาษาอังกฤษ (ฉบับเต็ม)

## 📞 ต้องการความช่วยเหลือ?

1. อ่านคู่มือ [SETUP_TH.md](./SETUP_TH.md)
2. ดูตัวอย่างใน [Example.gs](./Example.gs)
3. รัน `testLibrarySetup()` เพื่อตรวจสอบ
4. ตรวจสอบ Execution log ใน Apps Script
5. ดู logs ใน spreadsheet

## 🎓 เริ่มต้นเรียนรู้

### สำหรับผู้เริ่มต้น

1. อ่าน **SETUP_TH.md** - เข้าใจการติดตั้ง
2. ดู **Example.gs** - เรียนรู้จากตัวอย่าง
3. ทดลอง **quickTest()** - ทดสอบการใช้งาน
4. สร้างข้อมูลตัวอย่าง - ลองเอง

### สำหรับผู้พัฒนา

1. อ่าน **ARCHITECTURE.md** - เข้าใจระบบ
2. อ่าน **SCHEMA.md** - เข้าใจโครงสร้างข้อมูล
3. ศึกษาโค้ดใน Code.gs, Auth.gs, ฯลฯ
4. ปรับแต่งตามต้องการ

## ✨ Features เด่นๆ

- 🔐 **ปลอดภัยสูง** - Password hashing, Token expiration
- 📝 **Audit Trail** - บันทึกทุกการทำงาน
- 🎯 **Role-based** - แยกสิทธิ์ชัดเจน
- 🚀 **ใช้งานง่าย** - API เรียบง่าย
- 📊 **ครบครัน** - CRUD ทุกตาราง
- 🛠️ **บำรุงรักษาง่าย** - มี Maintenance functions
- 📚 **เอกสารครบ** - เข้าใจง่าย

## 🎯 Use Cases

### 1. ระบบจัดการบุคลากร
- จัดการข้อมูลพนักงาน
- ตำแหน่ง, ยศ, หน่วยงาน
- Login และ Authentication

### 2. ระบบ Dashboard
- แสดงข้อมูลสถิติ
- กรองตามหน่วยงาน
- Real-time data

### 3. Mobile App Backend
- RESTful-like API
- Token authentication
- Secure data access

### 4. Multi-app Data Sharing
- แอพหลายตัวแชร์ข้อมูล
- Authentication ด้วย app_key
- Centralized data

## 🔄 การอัปเดต

เมื่อมี version ใหม่:

1. อัปเดตไฟล์ .gs ใน Library Project
2. Deploy version ใหม่
3. อัปเดต version ในแอพที่ใช้ Library
4. ทดสอบการทำงาน

## 📝 License

MIT License - ใช้งานได้อย่างอิสระ

---

## 🚀 เริ่มต้นเลย!

พร้อมแล้ว? เริ่มจาก [SETUP_TH.md](./SETUP_TH.md)

มีคำถาม? ดู [Example.gs](./Example.gs)

---

**เวอร์ชัน:** 1.0.0  
**อัปเดต:** พฤศจิกายน 2568  
**ทีมพัฒนา:** DTP NST Team

**ขอให้ใช้งานได้อย่างมีประสิทธิภาพ! 🎉**
