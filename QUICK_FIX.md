# 🎯 วิธีแก้ปัญหา "Admin not found" แบบง่ายที่สุด

## ปัญหา
เวลาเรียก `request_token()` จาก client script ได้ `Admin not found`

## สาเหตุ
Library ไม่รู้ว่า spreadsheet ข้อมูลอยู่ที่ไหน

---

## ✅ วิธีแก้ (แค่ 2 ขั้นตอน)

### 1️⃣ หา Spreadsheet ID

เปิด spreadsheet ของคุณ แล้วดู URL:

```
https://docs.google.com/spreadsheets/d/1abc...xyz/edit
                                      ^^^^^^^^^^^
                                      ส่วนนี้คือ ID
```

**ตัวอย่าง:**
```
URL: https://docs.google.com/spreadsheets/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit

ID = 1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms
```

### 2️⃣ ใส่ ID ใน Sheet.gs

เปิดไฟล์ `Sheet.gs` หา**บรรทัดที่ 13:**

```javascript
const SPREADSHEET_ID = '';  // ⬅️ ว่างเปล่า
```

**เปลี่ยนเป็น:**

```javascript
const SPREADSHEET_ID = '1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms';  // ⬅️ ใส่ ID ของคุณ
```

**เสร็จแล้ว!** 🎉

---

## 🧪 ทดสอบ

### Test 1: จาก Library (Local)
```javascript
testSimple();
```

ควรได้: `✅ ทดสอบผ่านหมด!`

### Test 2: จาก Client Script
```javascript
function myFunction() {
  const adminToken = dptnstlib.request_token({
    username: 'admin',
    password: 'admin123'
  }, 'admin');
  
  console.log(adminToken);
}
```

**ผลลัพธ์ที่ต้องการ:**
```javascript
{
  success: true,
  token: "abc123...",
  expiresAt: "2025-11-10T08:00:00.000Z",
  message: "Authentication successful"
}
```

---

## 🔍 Troubleshooting

### ❌ ยังได้ "Admin not found"

**เช็ค:**
1. SPREADSHEET_ID ใส่ถูกหรือไม่ (ไม่มีช่องว่าง ไม่มี quotes ซ้อน)
2. Spreadsheet มี sheet ชื่อ "admins" หรือไม่
3. Sheet "admins" มีข้อมูล admin หรือไม่ (รัน `testSimple()` ใน library)

### ❌ "Permission denied"

**แก้ไข:** Share spreadsheet ให้ทุกคนที่มี link สามารถดูได้ (หรือ edit)

### ❌ "Spreadsheet not found"

**แก้ไข:** เช็ค ID ว่าคัดลอกถูกต้อง และ spreadsheet ยังมีอยู่ (ไม่ถูกลบ)

---

## 📝 หมายเหตุ

- ถ้า `SPREADSHEET_ID = ''` (ว่างเปล่า) → ใช้ `getActiveSpreadsheet()` (ทำงานแค่ใน local)
- ถ้า `SPREADSHEET_ID = 'xxx'` → ใช้ `openById()` (ทำงานทั้ง local และ client)

---

## ✅ สรุป

| ขั้นตอน | ทำอะไร | เวลา |
|---------|---------|------|
| 1 | Copy Spreadsheet ID | 10 วินาที |
| 2 | Paste ใน Sheet.gs | 5 วินาที |
| 3 | Save & Deploy | 30 วินาที |
| 4 | Test | 10 วินาที |
| **รวม** | **แก้เสร็จ** | **< 1 นาที** |

**ง่ายมาก ไม่ซับซ้อน!** 🚀
