# 🔧 คู่มือการตั้งค่า Config.gs

> จัดการการตั้งค่าทั้งหมดในที่เดียว - ง่าย เข้าใจง่าย

---

## 📋 Overview

ไฟล์ `Config.gs` รวบรวมตัวแปรตั้งค่าทั้งหมดของ Library ไว้ในที่เดียว ทำให้:
- ✅ **หาง่าย** - ไม่ต้องหาทีละไฟล์
- ✅ **แก้ไขง่าย** - เปลี่ยนที่เดียวใช้ได้ทั้งระบบ
- ✅ **ดูแลง่าย** - เห็นภาพรวมการตั้งค่าทั้งหมด

---

## 🎯 การตั้งค่าที่สำคัญ

### 1. SPREADSHEET_ID (⚠️ บังคับ)

```javascript
const SPREADSHEET_ID = '';
```

**ทำไมต้องตั้ง:**
- Library ต้องรู้ว่าจะเข้าถึง Spreadsheet ไหน
- ถ้าไม่ตั้ง จะใช้ได้แค่ในไฟล์เดียวกับ Spreadsheet เท่านั้น
- ถ้าเรียกจาก script อื่นจะ error "Admin not found"

**วิธีหา Spreadsheet ID:**

```
1. เปิด Google Spreadsheet ของคุณ
2. ดู URL ในแถบ address bar:

https://docs.google.com/spreadsheets/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit
                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                                      ส่วนนี้คือ SPREADSHEET_ID

3. คัดลอกมาใส่ใน Config.gs:

const SPREADSHEET_ID = '1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms';
```

---

### 2. TOKEN_EXPIRY_HOURS (อายุ Token)

```javascript
const TOKEN_EXPIRY_HOURS = 24;  // Default: 24 ชั่วโมง
```

**การใช้งาน:**
- เปลี่ยนเป็น `48` ถ้าต้องการ token อายุ 2 วัน
- เปลี่ยนเป็น `1` ถ้าต้องการความปลอดภัยสูง (ต้อง login ใหม่บ่อย)
- เปลี่ยนเป็น `168` ถ้าต้องการ token อายุ 1 สัปดาห์

---

### 3. PASSWORD_HASH_METHOD (วิธี Hash Password)

```javascript
const PASSWORD_HASH_METHOD = 'base64';  // Default: base64 (เร็ว)
```

**ตัวเลือก:**
- `'base64'` - เร็วที่สุด (10x เร็วกว่า SHA-256) เหมาะสำหรับ internal use
- `'sha256'` - ปลอดภัยกว่า แต่ช้ากว่า เหมาะสำหรับ production

**เมื่อไหร่ควรเปลี่ยน:**
- ใช้ `base64` สำหรับ: ทีมเล็ก, ใช้งานภายใน, ต้องการความเร็ว
- ใช้ `sha256` สำหรับ: ใช้งานจริง, มี user เยอะ, ข้อมูลละเอียดอ่อน

---

### 4. ENABLE_RATE_LIMITING (จำกัดจำนวน Request)

```javascript
const ENABLE_RATE_LIMITING = false;  // Default: off (Simple Mode)
```

**การใช้งาน:**
- `false` - ไม่จำกัด request (เร็วกว่า, ง่ายกว่า)
- `true` - จำกัด request ป้องกัน brute force attack

**แนะนำ:**
- Small team / Internal use → `false`
- Public facing / Many users → `true`

---

### 5. ENABLE_CACHE (ใช้ Cache)

```javascript
const ENABLE_CACHE = false;  // Default: off (Simple Mode)
```

**การใช้งาน:**
- `false` - อ่านจาก Sheet ทุกครั้ง (เร็วอยู่แล้ว ~50ms)
- `true` - ใช้ cache เพิ่มความเร็ว (แต่ซับซ้อนกว่า)

**แนะนำ:**
- User น้อย → `false` (ง่ายกว่า)
- User เยอะ (>100) → `true` (เร็วกว่า)

---

### 6. ENABLE_LOGGING (บันทึก Log)

```javascript
const ENABLE_LOGGING = true;   // Default: on
const LOG_LEVEL = 'INFO';      // ERROR, WARN, INFO, DEBUG
```

**การใช้งาน:**
- `true` - เปิด logging (แนะนำ)
- `false` - ปิด logging (เร็วกว่าเล็กน้อย แต่ debug ยาก)

**Log Level:**
- `ERROR` - เฉพาะ error
- `WARN` - เตือน + error
- `INFO` - ข้อมูลทั่วไป + เตือน + error
- `DEBUG` - ทุกอย่าง (ใช้เวลา debug)

---

## 🔄 วิธีใช้งาน Config

### ในโค้ดอื่นๆ เรียกใช้ผ่าน `Config` object:

```javascript
// ใน Sheet.gs
if (Config.SPREADSHEET_ID) {
  ss = SpreadsheetApp.openById(Config.SPREADSHEET_ID);
}

// ใน Helpers.gs
const hours = Config.TOKEN_EXPIRY_HOURS || 24;
now.setHours(now.getHours() + hours);

// ใน Security.gs
if (Config.ENABLE_RATE_LIMITING) {
  // ตรวจสอบ rate limit
}
```

---

## 📝 Checklist การตั้งค่าครั้งแรก

เมื่อติดตั้ง Library ครั้งแรก ให้ตรวจสอบ:

- [ ] ✅ ตั้ง `SPREADSHEET_ID` (บังคับ)
- [ ] ✅ ตรวจสอบ `TOKEN_EXPIRY_HOURS` (24 ชม. เหมาะสมหรือไม่)
- [ ] ✅ เลือก `PASSWORD_HASH_METHOD` (base64 หรือ sha256)
- [ ] ✅ ตัดสินใจ `ENABLE_RATE_LIMITING` (ต้องการหรือไม่)
- [ ] ✅ ตัดสินใจ `ENABLE_CACHE` (ต้องการหรือไม่)
- [ ] ✅ ตั้ง `LOG_LEVEL` (INFO หรือ DEBUG)

---

## 🔐 Security Recommendations

### Simple Mode (ปัจจุบัน)
```javascript
ENABLE_RATE_LIMITING = false
ENABLE_CACHE = false
PASSWORD_HASH_METHOD = 'base64'
```
**เหมาะสำหรับ:** ทีมเล็ก, ใช้งานภายใน, ต้องการความง่าย

### Production Mode (แนะนำสำหรับใช้งานจริง)
```javascript
ENABLE_RATE_LIMITING = true
ENABLE_CACHE = true
PASSWORD_HASH_METHOD = 'sha256'
```
**เหมาะสำหรับ:** User เยอะ, ข้อมูลสำคัญ, ต้องการความปลอดภัยสูง

---

## 🚨 Troubleshooting

### ❌ "Admin not found" error
**สาเหตุ:** ไม่ได้ตั้ง `SPREADSHEET_ID`
**แก้ไข:** เปิด Config.gs ใส่ Spreadsheet ID

### ❌ Token หมดอายุเร็วเกินไป
**สาเหตุ:** `TOKEN_EXPIRY_HOURS` น้อยเกินไป
**แก้ไข:** เปลี่ยนเป็น 48 หรือ 168 ชั่วโมง

### ❌ Login ช้า
**สาเหตุ:** ใช้ `PASSWORD_HASH_METHOD = 'sha256'`
**แก้ไข:** เปลี่ยนเป็น `'base64'` ถ้าไม่จำเป็น

---

## 📚 เอกสารเพิ่มเติม

- [README.md](./README.md) - คู่มือหลัก
- [Sheet.gs](./Sheet.gs) - Database Layer
- [Auth.gs](./Auth.gs) - Authentication
- [Security.gs](./Security.gs) - Security Functions

---

**💡 Tip:** แก้ Config.gs ครั้งเดียว ใช้ได้ทั้งระบบ ไม่ต้องหาทีละไฟล์!
